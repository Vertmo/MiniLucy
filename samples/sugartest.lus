node test_arrow(init : int) returns (z : int);
let
  z = init -> 2 * pre(z);
tel;
  
node test_arrow_clock(x : int; b : bool) returns (z : int when True(b));
let
  z = (x when True(b)) -> (2 * (x when True(b)));
tel;
  
node test_pre() returns (x : real);
let
  x = pre(x)+1.;
tel;