node test1() returns (i : int);
let
  var x : int;
  let
    x = 2;
    i = x + 1;
  tel
tel
  
node test2() returns (i : int);
let
  var x, y : int;
  let x = 2; y = 3;
      i = x + y;
  tel
tel

node test3() returns (i : int);
let
  var x : int;
  let x = 0; i = 41 + 1;
  tel
tel

node test4() returns (i : int);
let
  var x : int;
  let x = 0 fby (x + 1);
      i = x;
  tel
tel